kind = "web"
previewPath = "/"
title = "AI Resume Checker"
version = "1.0.0"
id = "artifacts/resume-checker"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 22278

[services.development]
run = "pnpm --filter @workspace/resume-checker run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/resume-checker", "run", "build" ]
publicDir = "artifacts/resume-checker/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "22278"
BASE_PATH = "/"
